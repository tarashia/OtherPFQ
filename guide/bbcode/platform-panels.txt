[display=Show a pkmnpanel platform-style]Up to two icons are shown on the name bar, so for example my Charizard is from PF1 and is mega, but only shiny/male is shown. Everything customizable is at the top as variables: platform/name/berry bg colors, and show/hide options for the held item and berry button.

Future goals: Expand the name link to make the whole area clickable (padding? would need a new element to form the name bg, and need to ensure berry stays clickable). Add exp bar below name (decide if it shares colors with name/platform or gets its own vars). Prevent ready to evolve button from showing for owner.

[sc=platform-party][pkmnpanel=3mc8] [pkmnpanel=Jg30] [pkmnpanel=kv3l9][/sc]

[hide=Short-form, especially for signatures][sc=code][nobbcode][sc=platform-party][pkmnpanel=3mc8][/sc] [sc=platform-party][pkmnpanel=Jg30][/sc] [sc=platform-party][pkmnpanel=kv3l9][/sc]
[style]
/* These default to the VIEWER's skin colors */
/* To customize them, just set hex color codes instead */
--pparty-platform-base: var(--col-bg1);
--pparty-platform-border: var(--col-border1);
--pparty-name-color: var(--col-fg1);
--pparty-name-background: var(--col-bg3);
--pparty-name-border: var(--col-border3);
--pparty-berry-background: var(--col-flavour-up);
--pparty-berry-border: var(--col-fg1);
/* Set to "block" to show, "none" to hide */
.platform-party .helditem { display: block; } /* held item */
.platform-party .action { display: block; } /* berry buttons */
[/style]
[import-style=b7q/sharedcss-new][/nobbcode][/sc][/hide]
[hide=Full code, for advanced users][sc=code][nobbcode][sc=platform-party][pkmnpanel=3mc8][/sc] [sc=platform-party][pkmnpanel=Jg30][/sc] [sc=platform-party][pkmnpanel=kv3l9][/sc]
[style]
/* These default to the VIEWER's skin colors */
/* To customize them, just set hex color codes instead */
--pparty-platform-base: var(--col-bg1);
--pparty-platform-border: var(--col-border1);
--pparty-name-color: var(--col-fg1);
--pparty-name-background: var(--col-bg3);
--pparty-name-border: var(--col-border3);
--pparty-berry-background: var(--col-flavour-up);
--pparty-berry-border: var(--col-fg1);
/* Set to "block" to show, "none" to hide */
.platform-party .helditem { display: block; } /* held item */
.platform-party .action { display: block; } /* berry buttons */

.platform-party:has(>.party), .platform-party .party {
  display: inline-block;
}
.platform-party .party {
  &,>div {
    padding: 0;
    margin: 0;
    background: transparent;
    width: 120px !important;
    height: 120px;
    overflow: hidden;
    border: none;
    border-radius: 0;
    box-shadow: none;
  }
  >div {
    &::after {
      display: none;
    }
    .pkmn {
      margin: 0 10px;
      float: unset;
      &::before {
        background-color: var(--pparty-platform-base);
        border-color: var(--pparty-platform-border);
      }
      .helditem {
        .itemsprite {
          cursor: unset;
        }
        .tooltip_content {
          display: none;
        }
      }
    }
    .name {
      background-color: var(--pparty-name-background);
      border: 1px solid var(--pparty-name-border);
      border-radius: 5px;
      margin-top: 1px;
      height: 16px;
      display: flex;
      flex-wrap: nowrap;
      a {
        color: var(--pparty-name-color);
        font-weight: normal;
        margin: 0 2px;
        width: 80px !important;
        flex-grow: 99;
        font-size: 12px;
      }
      img {
        display: none;
      }
      img:nth-last-of-type(-n+2) {
        display: inline;
        margin: 0;
      }
    }
    .action {
      position: absolute;
      left: 88px;
      top: 70px;
      height: unset;
      width: unset;
      .berrybuttons[data-up="any"]>a[data-berry="aspear"],
      .berrybuttons[data-up="sour"]>a[data-berry="aspear"], 
      .berrybuttons[data-up="spicy"]>a[data-berry="cheri"], 
      .berrybuttons[data-up="dry"]>a[data-berry="chesto"], 
      .berrybuttons[data-up="sweet"]>a[data-berry="pecha"],
      .berrybuttons[data-up="bitter"]>a[data-berry="rawst"] {
        display: inline-block;
      }
      .berrybuttons {
        >a {
          display: none;
          width: 20px;
          height: 20px;
          line-height: 20px;
          padding: 5px;
          background-color: var(--pparty-berry-background);
          border-radius: 20px;
          >img { 
            vertical-align: top;
            max-height: 20px;
            max-width: 20px;
          }
        }
        >a[data-berry="pecha"] img {
          /* this berry is short */
          margin-top: 1px;
        }
      }
      &.working {
        opacity: 0.5;
      }
      table, a[data-evolve] {
        /* "thank you" message and evolve button */
        display: none;
      }
    }
    .expbar, .taste, .extra {
      display: none;
    }
  }
  >div:hover>.action a[data-berry]:after {
    border-color: var(--pparty-berry-border);
  }
}
[/style][/nobbcode][/sc][/hide][/display]

[style]
--pparty-platform-base: var(--col-bg1);
--pparty-platform-border: var(--col-border1);
--pparty-name-color: var(--col-fg1);
--pparty-name-background: var(--col-bg3);
--pparty-name-border: var(--col-border3);
--pparty-berry-background: var(--col-flavour-up);
--pparty-berry-border: var(--col-fg1);
[/style]